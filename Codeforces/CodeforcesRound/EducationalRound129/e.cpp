#include <iostream>
#include <math.h>
#include <utility> 
int n; 
std::pair <int, int> a[100005], b[100005]; 
int numQueries; 
long long f[100005][17][2]; 

int main () {
  std::cin >> n;
  for (int i = 1; i < n; i++) {
    std::cin >> a[i].first >> a[i].second; 
    std::cin >> b[i].first >> b[i].second; 
  }
  for (int i = 1; i <= n; i++) {
    
  }
  return 0; 
}