#include <bits/stdc++.h>

using namespace std;
const long long MOD = 1000000007;
const int MAX_N = 3005;
long long dp[MAX_N][MAX_N];

class BFSOrder {
  public:
  int count(vector <int> bfsOrder) {
    int n = (int) bfsOrder.size();
    
  }
};

int main () {
  return 0;
}